# Codex Desktop -- holistic review prompt: PR #218 References & Values catalog expansion

Paste the prompt below into the Codex desktop app. It is self-contained. The branch is
`feat/matrix-options-refs-values-catalog-2026-05-31` (tip `58f97bd`) in the repo
`JasenNelson/SSTAC-Dashboard`; review its diff against `origin/main`.

If Codex desktop has repo access, point it at PR #218 directly:
https://github.com/JasenNelson/SSTAC-Dashboard/pull/218
Otherwise, run locally and feed it the diff:
`git -C C:\Projects\SSTAC-Dashboard-worktrees\refs-values-integration-2026-05-31 diff origin/main`

---

## PROMPT (paste below this line)

You are doing an adversarial HOLISTIC code + data review of a single integrated pull
request (#218) for a Next.js / TypeScript regulatory dashboard. This PR expands the
Matrix-Options "References & Values" evidence-library catalog by 478 new toxicity
reference value (TRV) candidate records, generated by a deterministic script from
extracted authoritative payloads (BC Protocol 28, US EPA IRIS, Health Canada TRV v4.0).

Be skeptical and specific. For every finding give: severity (P1 blocker / P2 should-fix /
P3 nit), the file + record id or line, the exact problem, and a concrete fix. If you
disagree with a design choice, argue it with evidence. Conclude with an overall verdict:
GREEN (mergeable), YELLOW (mergeable after listed P2 fixes), or RED (blocker present).

### Diff shape (7 files, +34734 / -10)
- `matrix_research/reference_catalog/human_health_trv_values.json` (+32004): 478 new
  ParameterValueRecord rows appended (file goes from 84 to 586 records).
- `matrix_research/reference_catalog/sources.json` (+31): one new source record
  `src-bc-protocol-28-2021-jan`.
- `scripts/matrix-options/generate-catalog-records.mjs` (+411): the deterministic generator.
- `src/lib/matrix-options/provenance/__tests__/epa_iris_canonical_snapshot.json` (+/-2240):
  expanded EPA IRIS canonical snapshot (179 records / 101 substances) used by a guardrail test.
- `src/lib/matrix-options/provenance/library.ts` (+/-26): a review-disposition guard fix.
- `src/lib/matrix-options/provenance/__tests__/{library.test.ts,catalog.test.ts}` (+/-32):
  hardcoded catalog-count assertions updated to the integrated totals.

### Domain rules that MUST hold (these are load-bearing project invariants)
1. UNITS ALWAYS NORMALIZED. Every value is stored in the canonical base unit for its
   input_key: oral RfD -> mg/kg-bw/day; oral slope factor -> per mg/kg-bw/day (i.e.
   (mg/kg-day)^-1); inhalation RfC -> mg/m3; inhalation unit risk -> per ug/m3. The
   generator must convert ug->mg (x1e-3), handle the Greek-mu (U+03BC) and micro-sign
   (U+00B5) BEFORE lowercasing (else a microgram silently becomes 'g' -> 1000x error),
   and handle reciprocal units for slope factor / unit risk. Look hard for ANY record
   whose magnitude is off by ~1000x or whose reciprocal direction is inverted. A
   real prior bug in this lane was exactly a unit-blind 1000x error.
2. AI NEVER SETS A CALCULATOR DEFAULT. Every NEW record must have
   default_status == 'available_option' (never 'current_default') and
   qa_status == 'needs_review'. Flag any new record that violates this.
3. AUTHORITATIVE SOURCE, NEVER MEMORY. IRIS records are validated within 2% of the EPA
   canonical snapshot by a guardrail test. Confirm the guardrail logic is sound (matches
   by substance_key::input_key, normalizes units before comparing) and that no IRIS record
   was shipped that is NOT within 2% of its snapshot anchor. Known-excluded as
   data-integrity mismatches (should NOT appear among new IRIS records): benzo_a_pyrene
   oral SF=1 (EPA 2) and IUR=0.0006 (EPA 0.001); chromium_hexavalent oral SF=0.16 (EPA
   0.27) and IUR=0.011 (EPA 0.018); carbon_tetrachloride IUR=1.5e-5 (EPA 6e-6). Verify
   none of these wrong values slipped in.
4. REFERENTIAL INTEGRITY. Every record's source_ids must resolve in sources.json and
   equation_ids must resolve in equations.json. candidate_group_id must be exactly the
   4-part join pathway__substance_key__input_key__jurisdiction. No duplicate
   parameter_value_id anywhere across human_health_trv_values.json + parameter_values.json.
5. PLAIN ASCII ONLY (code point <= 127) in all source/doc/comment text. The snapshot
   fixture may legitimately store EPA raw strings; the generator source and any comments
   must be ASCII (non-ASCII written as Unicode escapes).

### What to scrutinize in the generator (generate-catalog-records.mjs)
- The unit normalization function: is it truly fail-closed (throws/skips on an
  unrecognized unit rather than emitting a wrong number)? Are the dose/air/reciprocal
  classifications robust against the messy real units in the payloads (e.g. "mg/kg-d",
  "mg/kg/day", "(ug/m3)-1", "(ug/cu.m)-1", "ug/kg-day", "per mg/kg-bw/day")?
- Idempotency: re-running --write must not duplicate or mutate existing records (dedup by
  parameter_value_id, including cross-file against parameter_values.json).
- Deterministic ids: same input -> same parameter_value_id; no collisions across the
  direct + food pathway emission for one source value.
- Source remap: payload source ids (src-us-epa-iris, src-health-canada,
  src-bc-protocol-28-2021-jan) remap to ids that exist in sources.json; IRIS remap is
  input-key-aware (oral_rfd -> rfd-table source; others -> chemical-details source).
- Does it correctly skip non-toxicity-value inputs (TEF/relative-potency, dermal
  absorption, exposure params) rather than mis-mapping them?

### What to scrutinize in the data (human_health_trv_values.json new records)
- Spot-check ~15 records spanning all three sources and all four input_keys: does the
  numeric value match the source_excerpt in evidence_items (accounting for unit
  normalization)? E.g. an excerpt "3.00E-01 mg/kg/d RfD" must yield value 0.3 with unit
  mg/kg-bw/day.
- Are oral values correctly emitted to BOTH human-health-direct and human-health-food
  pathways, while inhalation values go to human-health-direct ONLY?
- Any record where unit and value disagree, or where value_type/qa_status/default_status
  are inconsistent with the invariants above.
- Cross-source spreads: the catalog deliberately keeps multiple jurisdiction candidates
  per (substance,input,pathway) for QP comparison (Protocol 1 s4.4). These are NOT bugs;
  but flag any case where the SAME source+substance+input+pathway+jurisdiction appears
  twice (that WOULD be a duplicate).

### What to scrutinize in library.ts
- The changed review-disposition logic: it should ensure a robot-extracted record with
  evidence_support_status='approved_source_backed' but canonical_source_status indicating
  unverified (needs_direct_source_check) and qa_status='needs_review' does NOT render as
  an "approved" disposition. Confirm the guard is correct and has no false-negative.

### Output
1. Findings list (severity, location, problem, fix).
2. Specifically answer: did you find ANY unit/magnitude error (esp. ~1000x or inverted
   reciprocal)? Any record violating the no-default / needs-review invariant? Any
   unresolved source/equation ref or duplicate id? Any of the known-bad
   BaP/Cr(VI)/carbon_tetrachloride values present?
3. Overall verdict: GREEN / YELLOW (with the P2 list) / RED (with the blocker).
