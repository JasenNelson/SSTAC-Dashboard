<!-- Generated 2026-07-01 by the mo-audit-and-excluded-proposal workflow (24 Sonnet agents, adversarial-verify). REPORT/PROPOSAL ONLY -- no library/catalog mutation. HITL review required before any fix or wiring. -->

# Matrix-Options SUBSTANCE_LIBRARY Audit Report

**Scope:** 389 entries in `src/lib/matrix-options/substanceLibrary.ts`, cross-checked against `matrix_research/reference_catalog/human_health_trv_values.json` and `parameter_values.json`. All candidate findings from the mechanical scan were independently re-verified by a second agent against the live repo before inclusion here.

**Result:** 16 confirmed findings out of 389 entries audited (~4%). Severity breakdown (post-verification, adjusted): **3 high**, **10 medium**, **3 low**. Zero findings involve a wrong number reaching a calculator output -- all confirmed issues are either (a) a wired value that diverges materially from the catalog's approved TRV for that endpoint, or (b) a `sources`/`notes` string that misattributes or goes stale relative to the tracked catalog provenance. No entries were found to have fabricated or nonexistent source citations outright.

## Findings

| Key | Severity | Issue | Detail |
|---|---|---|---|
| copper | high | value-vs-catalog | Wired oral RfD 0.04 mg/kg-bw/day cites "US EPA IRIS Cu," but IRIS has no oral RfD row for copper in the catalog at all, and the only approved row (Health Canada TDI) is 0.426 -- >10x higher. The 0.04 traces only to an unverified scaffold row (`src-current-calculator-design-v1`, explicitly flagged "not source-approved"). Unsubstantiated citation + large magnitude divergence on a human-health default. |
| benzo_a_pyrene | high | value-vs-catalog | Wired oral SF 1.0 cites "US EPA IRIS B[a]P (2017 reassessment)," but the approved IRIS value is 2 and the approved Health Canada value is 1.289 -- 1.0 matches neither. Traces to a self-referential legacy scaffold row, not an actual IRIS extraction. Affects a cancer slope factor used in risk math. |
| total_pcbs_aroclor_1254 | high | provenance-notes | Wired oral SF 2.0 has no catalog row under `total_pcbs_aroclor_1254` of any status. The value is cross-key-borrowed from the generic-mixture substance `polychlorinated_biphenyls_pcbs` (a different CAS) without disclosure; the `sources` string ("US EPA IRIS Aroclor 1254...") falsely implies Aroclor-1254-specific IRIS data. |
| lead | medium | value-vs-catalog | Wired oral RfD 0.0035 diverges ~7x from the approved Health Canada value (0.0005) and is not backed by an approved catalog row; it matches only a needs_review scaffold row citing a generic placeholder source, not the "Health Canada HHRA 2023" cited in `sources`. (Downgraded from the initial "matches nothing" framing -- a tracked scaffold row does exist, but it is unapproved and diverges from the approved TRV.) |
| arsenic_inorganic | medium | dual-source-citation | Wired RfD (0.0003) and SF (1.5) exactly match BC Protocol 28 needs_review rows, but `sources` credits only "US EPA IRIS iAs," which has materially different approved values (RfD 5x lower, SF 21x higher). Mis-citation, not just an undisclosed build-first choice (contrast with antimony/cobalt, which correctly disclose BC-P28 provenance). |
| naphthalene | medium | provenance-notes | `abs_dermal` is 0.148 vs the organic-PAH class default of 0.13 used by every sibling entry. No catalog row of any status supports 0.148, and unlike other chemical-specific overrides in the file, the deviation is never disclosed inline. Possible transcription error. |
| dichloromethane | medium | provenance-notes | Wired RfD/SF exactly match the approved Health Canada TRV v4.0 row, but `sources` leads with "US EPA IRIS toxicity values," which has different approved values -- backwards attribution (copy-paste boilerplate). |
| dichloroethylene_1_1 | medium | provenance-notes | Same misattribution pattern as dichloromethane: wired RfD (0.003) is 100% HC-sourced (matches exactly), but `sources` credits IRIS, whose approved RfD (0.05) is 16.7x different. Falls outside the mechanical dual-source check because SF is null here. |
| pentachlorophenol | medium | provenance-notes | `notes` claims the IRIS RfD/SF rows "remain needs_review," but both are now qa_status=approved in the catalog. Stale audit-trail text; values themselves are correct. |
| 1_4_dioxane | medium | dual-source-citation | RfD and SF are correct and both approved, but come from two distinct source_ids; the `sources` string cites neither literally, breaking the citation convention used elsewhere in the file (e.g. RDX, acrylamide). |
| isophorone | medium | provenance-notes | `notes` says RfD/SF rows "remain needs_review," but all four referenced rows are now qa_status=approved (promoted same day, after the library commit landed). Stale text; values correct. |
| 2_4_6_trinitrotoluene_tnt | medium | dual-source-citation | RfD and SF are both approved but from two distinct source_ids; `sources` says only "US EPA IRIS toxicity values" with no literal ids, pre-dating the stricter dual-source-id convention a later batch (Batch H) established after codex flagged the identical RDX pattern. |
| formaldehyde | medium | provenance-notes | `notes` says "oral RfD/SF candidates approved," but `sf_oral` is null and the catalog has no oral SF row for formaldehyde at all (only RfD plus inhalation UR/RfC). Leftover template wording copied across an 8-entry batch; no functional impact since SF is correctly excluded. |
| 1_4_dioxane | low | provenance-notes | Same "remain needs_review" staleness as isophorone/pentachlorophenol -- both referenced rows are now approved. Documentation-only; values correct. |
| isophorone | low | dual-source-citation | Same missing-literal-source-id gap as 1,4-dioxane/TNT, but full traceability is preserved via `notes`, which does cite the specific parameter_value_ids. Legacy pre-convention entry. |
| bis_2_ethylhexyl_phthalate_dehp | low | dual-source-citation | RfD/SF are correct, both approved, from two distinct source_ids, but `sources` says only "US EPA IRIS toxicity values" with no literal ids -- same legacy Batch-E gap as TNT, fully traceable via the JSON catalog itself. |

## No-action / false positives filtered

None of the 16 items provided were filtered out -- every finding in the input list was independently re-confirmed as real (`verdict.real: true`) against the live repo. The only adjustments made during verification were to **severity** (several downgraded from the initial "high"/"medium" flag once cross-checked against the full catalog, e.g. lead high->medium, several provenance-notes items medium->low) and to **framing** (e.g. the lead finding's original "matches nothing in the catalog" claim was corrected to "matches an unapproved scaffold row, diverges from the approved TRV," since an undisclosed row does in fact exist in `parameter_values.json`). No confirmed finding involves a value that actually reaches a calculator with incorrect math; all are either divergent-from-approved defaults pending reconciliation, or citation/documentation accuracy gaps in the `sources`/`notes` audit trail.
