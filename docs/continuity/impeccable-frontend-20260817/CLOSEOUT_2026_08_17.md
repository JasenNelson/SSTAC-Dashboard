# CLOSEOUT -- Impeccable frontend lane, SSTAC-Dashboard, 2026-08-17

LANE STATUS: COMPLETE. All six product PRs merged; landed tree gated and verified. This lane is
closed and must not be reopened for implementation, review findings, UI work, or deferred issues.

Companions in this directory: `RESUME.md` (fresh-session entry point),
`AUTHORIZATION_PACKET_2026_08_17.md` (the pre-merge owner decision record),
`MERGE_READINESS_PACKET_2026_08_17.md` (the inherited checkpoint this session started from),
`EVIDENCE_MANIFEST.json` + `EVIDENCE_SHA256.txt` (what was preserved and its hashes),
`evidence/` (the artifacts themselves).

## 1. Outcome

    origin/main   120c6f9a04de7e689c54579919f60144466e7f79
              ->  0ef90f48b663dbab3af7e1527995ff1747896b33

    #782 wave0         merged   merge commit 22ef4532
    #787 theme-cookie  merged   via wave0 -- ATOMICALLY with #782
    #783 batch2        merged   merge commit 0ef90f48
    #784 waveA         merged   via batch2
    #785 triage        merged   via wavea
    #788 p0            merged   via deferred-triage
    #786               OPEN, docs-only, deliberately outside this lane (head 7e465cca)

Exact per-PR merge commits, bases, and merge timestamps:
`evidence/verification/pr-commit-mapping-and-merge-evidence.md`.

The coupled pair landed atomically, so the `main` BRANCH never had an intermediate state containing
#782 without #787. That is a statement about git history, not about users: no deployment occurred,
so actual user exposure was never measured and is not claimed. This was the single most important
sequencing decision in the lane.

## 2. What this lane actually changed, and why it mattered

The session inherited a checkpoint that presented both stacks as gated and merge-ready. Independent
review found three P1 defects that a full green six-gate battery had already passed. All three were
the same defect class this repository has been bitten by repeatedly: a green suite coexisting with
the SILENT LOSS OF A CORRECT REGULATORY DISCLOSURE, rather than corruption of a value.

P1-1  `TWGReviewPortal.tsx` -- clicking **Save Draft** permanently destroyed the unknown-provenance
      disclosure that PR #788 exists to create. A legacy draft (exactly 5000 characters, no
      truncation key -- a real population, because `main` as it stood before this lane carried
      `maxLength={5000}`)
      correctly warned that an unknown amount of text may be missing. One Save wrote an empty `{}`
      truncation record. On the next mount the `rawT !== null` branch discarded the at-limit keys,
      and per the component's own comment `{}` then positively asserts that nothing was lost. The
      submit gate passed and possibly-clipped regulatory review text could reach `matrix_reviews`
      with no disclosure at all.
      FIX: persist the unknown-provenance set under its own storage key so a `{}` truncation record
      keeps its exact meaning while the disclosure survives independently.

P1-2  The cancelled-submit note grew the absolutely-positioned bottom bar past the scroll
      container's fixed `pb-32` (128px) clearance, occluding the Dismiss controls the note itself
      instructs the reviewer to use -- and `handleCommentChange` deliberately never clears the
      record, so there was no other way out.
      FIX: bound the note (`max-h-40` + `overflow-y-auto` + `tabIndex={0}` so it is keyboard
      reachable) and raise the with-note reservation to `pb-72`; the arithmetic (244px bar worst
      case against 288px reserved) is documented in-code.

P1-3  The same disclosure was ALSO erased by a FAILED save, and the code comment justifying the
      write ordering was factually wrong for exactly the population the feature targets.
      FIX: write order is now antidote (unknown-provenance) -> poison (truncation) -> draft, with
      any failure aborting the save so no resumable-but-undisclosed draft can exist.

Two further corrections, test-and-truth only:
  - the `unresolvedCensoring` argument that `stats.ts` passes to `recommendUcl` is now PINNED by a
    test at the call site. Before, deleting it would have silently restored the fail-open path -- a
    false ProUCL provenance citation on a screening estimate -- with every existing test still
    green. Falsification showed the fail-open value was `chebyshev95` with a ProUCL citation.
  - three regression tests that #787's rebase had DELETED while their production code remained were
    restored (this project's CLAUDE.md forbids that outright), and `src/lib/theme.ts` was made to
    genuinely satisfy its own claim of being the single source of truth for the theme value set.

Every new or restored test was falsified two-sided: the bug reintroduced, the exact failure message
recorded, the fix restored. See `evidence/corrections/`.

A fourth, process-level finding: `.github/workflows/ci.yml` has no `types:` key, so retargeting a
stacked PR to `main` emits `edited` and would NEVER have fired the four required contexts. Following
the inherited plan would have left all four stacked PRs permanently blocked. This is why the merge
strategy changed to bottom-up parent-branch aggregation.

## 3. Evidence

### Landed tree -- main @ 0ef90f48, six gates, ALL GREEN

    lint 0 | tsc clean | unit 6935 passed / 19 skipped / 2 todo (364 files)
    build exit 0, BUILD_CORROBORATION=OK
    e2e 215 passed / 0 failed, 32 chromium-auth refs
    docs gate PASS
    ALL_SIX_GATES_RAN=YES | TREE_UNCHANGED=YES

This was the FIRST gate ever run on the union of both stacks. The inherited checkpoint had gated
each stack separately against `main` and never their combination.
Receipt: `evidence/gates/landed-main-GREEN.txt`.

### Pre-merge combined trees

    stackA3  ae678fdf  unit 6871 | e2e 167/0   evidence/gates/combined-stackA3-GREEN.txt
    stackB2  887d9265  unit 6847 | e2e 208/0   evidence/gates/combined-stackB2-retry-GREEN.txt

Two first attempts went RED on e2e and each was retried once per the SOP. Both were flakes, and the
runner correctly refused to report an incomplete suite as green in both cases. The failing receipts
are preserved deliberately (`*-attempt1-RED.txt`) together with the causal-isolation reasoning in
`evidence/gates/flake-evidence.md`.

### Remote CI

Both root PRs were green on all four required contexts before merge. #783 additionally showed all
11 checks green. Measured proof of the ci.yml trigger defect: the pushed tips `887d9265` and
`ae678fdf` each carried exactly 2 checks, with no Lint / Unit / Build / E2E.

### Rendered browser

Production build of the landed tree. With JavaScript ABSENT, a `theme=dark` cookie yields
`<html class="dark">` and `aria-label="Switch to light mode"`; `theme=light` yields the inverse. In
headless Chromium the body computed background is `rgb(15, 23, 42)` and the toggle's measured
bounding box is 44x44 px -- geometry, not a class-name assertion. Print verified through Playwright
real print-media specs on the landed tree, including the two clipping guards.
Detail and limits: `evidence/verification/browser-verification.md`.

### Review pipeline

    Tier 0   cursor-agent --model auto        GREEN, 3 P2 + 1 P3 -- MISSED all three P1s
    Leg 1a   Opus subagent, round 1           RED, 2 P1
    Leg 1b   Opus context-inheriting fork     RED on the plan; found the 3rd P1
    Leg 1a   Opus subagent, round 2 (fresh)   GREEN, all 3 P1 closed, no vacuous tests
    Leg 2    codex gpt-5.6-luna high          silent bail, then 2 P1 + 1 P2 after re-framing
    Leg 2    codex gpt-5.6-sol xhigh          THE SHIP GATE -- ran, 1 P1 + 2 P2

The lane is FULLY codex-gated: the sol xhigh round ran and produced findings. This is NOT a
"luna GREEN, sol deferred on budget" case and must never be reported as one.
Adjudication of every codex finding: `evidence/reviews/codex-findings-and-adjudication.md`.
All rounds were also logged to the codex re-review queue at
`C:\Users\jasen\.claude\projects\C--Projects-Regulatory-Review\memory\codex_rereview_queue_2026_05_17.md`.

## 4. Claims deliberately NOT made

- **No deployment claim.** No deployed environment was exercised. All runtime evidence is local
  build of merged main, plus GitHub CI. There is no production or preview URL evidence.
- The TWG truncation/provenance surfaces are authenticated and were not driven by hand in a
  browser; they are covered by falsified unit tests and the landed e2e run.
- `TREE_UNCHANGED` is quoted as sound. An earlier claim in this session that it was vacuous (because
  the digest is the SHA-256 of the empty string) was WRONG and was retracted: `gates.sh
  tree_digest()` hashes porcelain + diff + cached + untracked bytes, so on a clean tree the empty
  digest is the correct value and would change if anything were dirty.

## 5. Accepted deferrals -- none blocking, all owner-decided

1. **Theme cookie-less migration window.** A returning browser with `localStorage.theme='dark'` and
   no cookie gets ONE request with a wrong ThemeToggle accessible name. Root cause: `layout.tsx`
   passes an authoritative `'light'` when the cookie is absent, so `seedTheme`'s DOM-class fallback
   at `ThemeContext.tsx:85` is unreachable in production. Self-heals on the next request. A fix
   would pass `undefined` when the cookie is ABSENT rather than present-and-light, which trades the
   wrong label for a first-render hydration mismatch -- a design decision, not a bug fix.
2. **TWG dismissal before any save is not durable** -- the next mount re-derives the notice.
   Over-warns only; cannot suppress a disclosure.
3. **TWG unpaired truncation marker** -- if the unknown-provenance record is selectively lost while
   the truncation record survives, the disclosure is discarded. This is a CODE-PATH assessment, not
   deployment evidence: the state is not reachable from the landed `main` tree or by any ordinary
   single-tab sequence.
4. Pre-existing and untouched by this lane: the `stats.ts` statistical TREATMENT decision for an
   unresolved censoring row; cross-tab concurrent save pairing a clipped draft with an empty
   truncation record; four BN-RRM findings (`RiskComparison.tsx:463` print clip, `:456` vs `:350`
   predicate mismatch, `BNRRMClient.tsx:438` count-after-slice, `memo-generator.ts` dead third-state
   guards); 26 remaining audit findings.
5. PR #786 left OPEN and out of scope.

## 6. Reopening conditions

Reopen a NEW lane (do not reopen this one) if any of the following becomes true:

- a user reports a wrong theme-toggle label that does NOT self-correct on the next page load, which
  would mean deferral 1 is worse than the one-request window this lane accepted;
- a TWG reviewer reports a submitted comment that was silently shortened, which would mean a path
  exists to erase the disclosure that this lane's three fixes did not close;
- a UCL result is reported that cites a ProUCL section for a dataset whose censoring status is not
  fully resolved, which would mean the fail-closed guard was bypassed or its call-site argument was
  dropped despite the pin;
- `censored` is ever made nullable in the schema, which is the exact event the fail-closed guard was
  written for and which should be paired with the deferred treatment decision (deferral 4);
- `ci.yml` gains a `types:` key or its trigger changes, which would invalidate the merge-strategy
  reasoning recorded here for any future stacked-PR family.

## 7. Rollback and hazards

    tag p787-prerebase-3a5eb26f  -> 3a5eb26fefaee9e7644dbca0a980987519b0069e   KEEP -- LOCAL ONLY
    tag p0-precommit-87b8d2c8    -> 87b8d2c82d0ade1f3208d01115a32f3eaccb20b8   KEEP -- LOCAL ONLY

NEITHER TAG WAS PUSHED TO ORIGIN. `git ls-remote origin refs/tags/...` returns nothing for either,
so these labels do not survive loss of this machine. The commits they point at are ancestors of
`main`, so the history itself is safe remotely; only the named rollback labels are local.

Both re-verified after the force-push. `main` was never force-pushed. Every pre-correction commit
(`04c9cc10`, `45293f1a`) is an ancestor of its corrected tip, so nothing was lost by landing.

Retained branches: `trial/stackA2`, `trial/stackA3`, `trial/stackB2`, `landed/main-verify`.

**JUNCTION HAZARD.** `C:/Projects/SSTAC-Dashboard-worktrees/trial-refreeze-20260817` was created by
this lane and its `node_modules` is a JUNCTION to the shared store
(`C:/Projects/SSTAC-Dashboard/node_modules`, 721 entries at creation). A recursive delete or
`git worktree remove` FOLLOWS the junction and empties the shared store. Remove the junction first
(verify with `Get-Item ... | LinkType`, not a `dir /AL` count), confirm the store's child count is
unchanged, then delete. See L0 rule 1.15.

## 8. Process state

No session-owned processes remain. The production server on port 3544 and the headless browser were
both stopped and the port confirmed closed. Long-lived `codex` / `node` / `python` processes on this
machine belong to other live sessions and were never touched; per standing rule, parallel AI
sessions are not a blocker and a process census is not a gate.
